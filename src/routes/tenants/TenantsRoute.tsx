import { SectionPlaceholder } from '../common/SectionPlaceholder'
import { UI_PAGE_SUBTITLE_TENANTS, UI_PAGE_TITLE_TENANTS } from '../../ui/constants'

export function TenantsRouteComponent() {
  return <SectionPlaceholder title={UI_PAGE_TITLE_TENANTS} subtitle={UI_PAGE_SUBTITLE_TENANTS} />
}

